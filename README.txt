msofficeconverter

A commandline tool to convert documents using Microsoft Office

requires Microsoft Office to be installed to use!

To build, open the solution in Visual Studio. Requires the correct Microsoft Office .NET interop library for the version being built.